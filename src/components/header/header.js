import React from 'react';
import NavBar from '../nav-bar/nav-bar';
import './header.css';

const Header = () => {
    return (
        <div>
            <NavBar />
        </div>
    );
};

export default Header;